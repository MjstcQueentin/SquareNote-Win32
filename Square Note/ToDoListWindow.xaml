<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Square_Note.ToDoListWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Square_Note"
    xmlns:objects="using:Square_Note.Objects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="To-Do List">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="31" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="WindowTitleBar" Grid.Row="0"></Grid>

        <Grid Padding="16 0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" FontSize="24" FontWeight="SemiBold" x:Name="TitleTextBlock"></TextBlock>
            <Button Grid.Column="1" ToolTipService.ToolTip="Options...">
                <Button.Content>
                    <FontIcon Glyph="&#xEDE3;" FontSize="16" />
                </Button.Content>
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Renommer la liste..." x:Name="EditTitleButton" Click="EditTitleButton_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Edit" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Supprimer la liste" x:Name="DeleteListButton" Click="DeleteListButton_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Delete" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Afficher la fenêtre principale" x:Name="DisplayMainWindowButton" Click="DisplayMainWindowButton_Click"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
        </Grid>

        <InfoBar IsOpen="True" IsClosable="False" Margin="16 0" Grid.Row="2" x:Name="EmptyListInfoBar">
            <InfoBar.Title>Aucun élément.</InfoBar.Title>
        </InfoBar>

        <ItemsRepeater x:Name="MyItemsRepeater" Grid.Row="3" Margin="16 0">
            <DataTemplate x:DataType="objects:ToDoListItem">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CheckBox IsChecked="{x:Bind Checked}" Content="{x:Bind Label}"
                              x:Name="ItemCheckbox" Click="ItemCheckbox_Click"
                              Tag="{x:Bind Index}"  Grid.Column="0" />
                </Grid>
            </DataTemplate>

            <ItemsRepeater.Layout>
                <StackLayout />
            </ItemsRepeater.Layout>
        </ItemsRepeater>

        <Grid Grid.Row="4" Padding="16" ColumnSpacing="8" Background="{StaticResource LayerOnMicaBaseAltFillColorDefault}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="NewItemTextBox" PlaceholderText="Nouvel élément" KeyDown="NewItemTextBox_KeyDown" />
            <Button Grid.Column="1" x:Name="AddItemButton" Click="AddItemButton_Click">
                <SymbolIcon Symbol="Add" />
            </Button>
        </Grid>
    </Grid>
</Window>
